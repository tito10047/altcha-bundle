altcha:
  enable: true
  hmacKey: '%env(APP_SECRET)%'
  floating: true
  include_script: true
  use_stimulus: false
  altcha_js_path: 'https://cdn.jsdelivr.net/npm/altcha/dist/main/altcha.min.js'
when@dev:
  altcha:
    enable: true
